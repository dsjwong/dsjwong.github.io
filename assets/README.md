# assets/

Put `resume.pdf` in this folder — the "Résumé" button in the hero links to
`assets/resume.pdf`.

If you do not have one ready yet, delete that button from `index.html` rather
than leaving it pointing at a 404.

Images go in `assets/img/`.
